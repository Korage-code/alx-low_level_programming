#include <stdio.h>
/**
 * main - Entry Point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int digits;

	for (digits = 0; digits <= 9; ++digits)
	{
		printf("%d", digits);
	}

	printf("\n");

	return (0);
}
